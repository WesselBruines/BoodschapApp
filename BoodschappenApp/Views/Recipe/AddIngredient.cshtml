@model IEnumerable<IngredientDB.RecipeIngredient>
@{
    ViewBag.Title = "AddIngredient";
}

<h2>Dit zijn de RecipeIngredients</h2>

<p>
    @Html.ActionLink("Maak nieuw Recipe Ingredient aan", "CreateRecipeIngredient")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ingredient.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ingredient.merk)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hoeveelheid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Eenheid)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ingredient.name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ingredient.merk)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Hoeveelheid)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Eenheid)
    </td>
    <td>
        @*@Html.ActionLink("Edit", "Edit", new { id = item.ingredientID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ingredientID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ingredientID })*@
        @Html.ActionLink("AddIngredient", "AddIngredient", new {id = item.RecipeIngredientID})
    </td>
</tr>
    }

</table>